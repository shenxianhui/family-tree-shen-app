<view class="course">
  <view class="course-content">
		<view
			wx:for="{{courseData}}"
			class="course-card"
			bindtap="handleCard"
			data-data="{{item}}"
		>
			<text class="title">{{item.title}}</text>
		</view>
	</view>
</view>
