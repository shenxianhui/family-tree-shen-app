<view class="menu-left">
	<view class="wrap">
		<view
			wx:for="{{list}}"
			wx:key="index"
			class="card"
			bindtap="handleBtn"
			data-data="{{item}}"
		>
			<view class="icon iconfont {{item.icon}}"></view>
			<view class="name">{{item.name}}</view>
		</view>
	</view>
</view>
